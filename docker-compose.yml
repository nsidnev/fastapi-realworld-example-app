version: '3'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - db
  db:
    image: postgres:11.5-alpine
    ports:
      - "5433:5432"
    env_file:
      - .env
    volumes:
      - ./dbdata:/var/lib/postgresql/data:cached
